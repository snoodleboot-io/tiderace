# Phase 1 — Environment Manifest (Lane 0)

Generated by the env-setup gate on 2026-06-10. Status: **all health checks GREEN**.
The pipeline provisioned everything below — no manual human setup was required.

## What was started / provisioned

| # | Item | Version | How it was provisioned | Health check (verify) | Status |
|---|------|---------|------------------------|------------------------|--------|
| E1 | Rust toolchain | cargo/rustc 1.95.0, clippy 0.1.95, rustfmt 1.9.0 | pre-installed | `cargo --version && cargo clippy --version && rustfmt --version` | ✅ |
| E2 | Isolated venv | CPython 3.12.3 | `uv venv .tiderace-bench-venv` (ensurepip absent → uv used instead of stdlib venv) | `./.tiderace-bench-venv/bin/python -V` | ✅ |
| E3 | pytest + coverage | pytest 9.0.3, coverage 7.14.1 | `uv pip install` into venv | `python -c "import pytest, coverage"` | ✅ |
| E4 | xdist + testmon | pytest-xdist 3.8.0, pytest-testmon 2.2.0 | `uv pip install` into venv | `pytest --help \| grep -- --testmon` and `-n numprocesses` | ✅ |
| E5 | hyperfine | 1.20.0 | `cargo install hyperfine` | `hyperfine --version` | ✅ |
| E6 | cargo-llvm-cov | 0.8.7 | pre-installed | `cargo llvm-cov --version` | ✅ |
| E7 | Reference fixture suite | 101 tests (96 pytest + 5 unittest), 6 modules | `python benchmarks/fixtures/generate.py` | `cd benchmarks/fixtures/sample_project && pytest -q` → `101 passed` | ✅ |
| E8 | Branch + session | `feat/tiderace-hardening-benchmarks`, `session_20260610_rt1h9b.md` | `git checkout -b` + session file | `git branch --show-current` | ✅ |

## How to verify the whole environment (one shot)

```bash
cd /home/snoodleboot/Documents/software/tiderace
cargo --version && cargo clippy --version && rustfmt --version
hyperfine --version && cargo llvm-cov --version
./.tiderace-bench-venv/bin/python -c "import pytest, coverage, xdist; print('py ok')"
./.tiderace-bench-venv/bin/python -m pytest -q benchmarks/fixtures/sample_project   # expect: 101 passed
```

## How to stop / clean up

```bash
# venv + runtime state are gitignored and safe to delete
rm -rf .tiderace-bench-venv .tiderace.db .tiderace-coverage
# regenerate the fixture deterministically at any time
./.tiderace-bench-venv/bin/python benchmarks/fixtures/generate.py
```

## Baseline evidence captured during the gate (for Lane A)

- **W1 (impact reliability):** with `--coverage`, a no-change warm run skipped 43/101 but re-ran 58 (should approach 96). Dep graph populates `test_file_deps` (1188 rows).
- **W3 (coverage persistence):** `coverage_data` table has **0 rows** after a coverage run — confirmed dead code path.
- **W4 (unittest):** tiderace collects `tests/test_unit_case.py::test_scale_0` (class-less); pytest needs `::ArithmeticCase::test_scale_0`. The 5 unittest tests fail to run under tiderace today.

## Notes / deviations

- stdlib `python3 -m venv` failed (`ensurepip`/`python3.12-venv` not installed, sudo unavailable). Resolved without sudo by using the already-present `uv` (`/home/snoodleboot/.local/bin/uv`). No system packages were modified.
- All Python deps live in the isolated venv; system Python was left untouched.
